meta {
  name: Get Bookmark
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/bookmark/{{bookmarkId}}
  body: none
  auth: none
}

tests {
  test("status must be 200", function() {
    expect(res.status).to.eql(200);
  });
}
